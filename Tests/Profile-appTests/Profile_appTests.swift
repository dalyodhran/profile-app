import XCTest
@testable import Profile_app

final class Profile_appTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(Profile_app().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
